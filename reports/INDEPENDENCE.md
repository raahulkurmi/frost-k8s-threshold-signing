# Signer independence evidence (Phase 7B)

> **Topology: multi-VM, single physical host (Level 1). This is not infrastructure
> independence.** All four VMs run under one Multipass hypervisor on one Mac, with one
> disk, one power supply, one kernel-level host, one operator and one dealer. Level 2
> (separate regions or providers) is **NOT DONE**.

Generated by `deploy/multihost/gen-independence.sh` from:
- deploy manifest `reports/multihost/e2e-20260924T210809Z-45dfe68/deploy-manifest.json` (git commit `45dfe68b0896c4ba0ad52125d2495ec5dca3053b`, deployed 2026-09-24T21:08:39Z, kid `4pzW_MwX1eZYlV4oGbSxKw`)
- e2e evidence `reports/multihost/e2e-20260924T210809Z-45dfe68` (L1–L5 plus the coordinator-host e2e)
- benchmark `benchmark/results/20260925T094355Z-9bb1f16-multihost-L1`

## Per-signer placement

| Signer | Host (VM) | IP:port | Region / provider | OS user | Share held (index only) | TLS cert SAN | Cert SHA-256 | Signer binary SHA-256 on host |
|---|---|---|---|---|---|---|---|---|
| 1 | sig-a | 192.168.252.3:8441 | local Multipass VM on the operator Mac (same for all) | `frost-signer-1` | share-1 only | `signer-1` | `07:42:5E:F8:22:8F:CD:C1:10:9E:89:1F:C1:50:48:33:04:90:1B:3E:C1:E2:53:A5:6E:5E:E8:2A:15:CB:1F:95` | `445fb979a657f98d815cd7d8fff31c7d9309085cd7856f1741b08c7034095aa6` |
| 2 | sig-a | 192.168.252.3:8442 | local Multipass VM on the operator Mac (same for all) | `frost-signer-2` | share-2 only | `signer-2` | `1C:F8:10:9B:69:94:99:D4:B5:2A:38:A3:54:12:51:7D:A8:F1:81:43:81:F9:74:1E:53:69:1C:69:0F:15:9F:CA` | `445fb979a657f98d815cd7d8fff31c7d9309085cd7856f1741b08c7034095aa6` |
| 3 | sig-b | 192.168.252.4:8443 | local Multipass VM on the operator Mac (same for all) | `frost-signer-3` | share-3 only | `signer-3` | `61:27:E0:B9:C6:A8:6F:83:ED:30:CF:89:D1:1C:7A:28:6D:96:42:62:6F:69:6E:39:EE:81:B9:BD:AB:93:87:92` | `445fb979a657f98d815cd7d8fff31c7d9309085cd7856f1741b08c7034095aa6` |
| 4 | sig-b | 192.168.252.4:8444 | local Multipass VM on the operator Mac (same for all) | `frost-signer-4` | share-4 only | `signer-4` | `BC:10:F2:85:1F:B8:5F:51:10:A8:97:9E:FD:67:12:1B:F5:B6:78:11:24:CA:1E:C3:AE:B5:27:9F:96:9F:A6:7B` | `445fb979a657f98d815cd7d8fff31c7d9309085cd7856f1741b08c7034095aa6` |
| 5 | sig-c | 192.168.252.5:8445 | local Multipass VM on the operator Mac (same for all) | `frost-signer-5` | share-5 only | `signer-5` | `D2:60:F0:CE:E8:88:19:35:16:54:FB:EE:A7:D0:8D:2E:8A:01:9F:ED:9E:DF:B2:D8:72:16:F6:23:8C:11:96:4B` | `445fb979a657f98d815cd7d8fff31c7d9309085cd7856f1741b08c7034095aa6` |

Coordinator host: `tk8s` (`192.168.252.2`). It holds **no share**:
only `public-meta.json`, the CA certificate and the coordinator-side certs
(`coordinator`, `coordinator-grpc`, `lb`) (L4). The operator (dealer) machine is `192.168.252.1`;
the ceremony directory was deleted after distribution (L4).
Signer binary: go1.27.1 linux/arm64, SHA-256 `445fb979a657f98d815cd7d8fff31c7d9309085cd7856f1741b08c7034095aa6`,
identical on every host (L5).

## Firewall rules per signer host (nftables, default drop)

### sig-a
```
type filter hook input priority filter; policy drop;
iif "lo" accept
ct state established,related accept
ct state invalid drop
ip saddr 192.168.252.1 tcp dport 22 accept comment "ssh from operator only"
ip saddr 192.168.252.2 ip daddr 192.168.252.3 tcp dport { 8441, 8442 } accept comment "signer ports from coordinator host only"
ip saddr { 192.168.252.1, 192.168.252.2 } icmp type echo-request accept comment "ping for RTT measurement"
udp sport 67 udp dport 68 accept comment "DHCP lease renewal"
icmpv6 type { nd-router-advert, nd-neighbor-solicit, nd-neighbor-advert } accept
counter packets 46 bytes 2852 drop comment "default deny"
type filter hook forward priority filter; policy drop;
type filter hook output priority filter; policy accept;
```
### sig-b
```
type filter hook input priority filter; policy drop;
iif "lo" accept
ct state established,related accept
ct state invalid drop
ip saddr 192.168.252.1 tcp dport 22 accept comment "ssh from operator only"
ip saddr 192.168.252.2 ip daddr 192.168.252.4 tcp dport { 8443, 8444 } accept comment "signer ports from coordinator host only"
ip saddr { 192.168.252.1, 192.168.252.2 } icmp type echo-request accept comment "ping for RTT measurement"
udp sport 67 udp dport 68 accept comment "DHCP lease renewal"
icmpv6 type { nd-router-advert, nd-neighbor-solicit, nd-neighbor-advert } accept
counter packets 46 bytes 2852 drop comment "default deny"
type filter hook forward priority filter; policy drop;
type filter hook output priority filter; policy accept;
```
### sig-c
```
type filter hook input priority filter; policy drop;
iif "lo" accept
ct state established,related accept
ct state invalid drop
ip saddr 192.168.252.1 tcp dport 22 accept comment "ssh from operator only"
ip saddr 192.168.252.2 ip daddr 192.168.252.5 tcp dport 8445 accept comment "signer ports from coordinator host only"
ip saddr { 192.168.252.1, 192.168.252.2 } icmp type echo-request accept comment "ping for RTT measurement"
udp sport 67 udp dport 68 accept comment "DHCP lease renewal"
icmpv6 type { nd-router-advert, nd-neighbor-solicit, nd-neighbor-advert } accept
counter packets 29 bytes 1804 drop comment "default deny"
type filter hook forward priority filter; policy drop;
type filter hook output priority filter; policy accept;
```
In addition, each signer's systemd unit sets `IPAddressDeny=any` and
`IPAddressAllow=<coordinator host>`, and runs with `NoNewPrivileges`,
`ProtectSystem=strict`, `ProtectHome`, `PrivateTmp`, a system-call filter and an
empty capability set (L5). The coordinator host restricts the coordinators' own egress to
exactly the five signer IP:port pairs (`DOCKER-USER`, N4).

## What was tested

| Test | Claim | Result |
|---|---|---|
| L1 | No signer host can open TCP to another signer host's signer ports or SSH | PASS |
| L2 | SSH to signer hosts only from the operator; the operator cannot reach signer ports | PASS |
| L3 | Co-located signers (sig-a: 1,2; sig-b: 3,4) cannot read each other's share or TLS key (0600, own user) | PASS |
| L4 | Each host holds exactly its assigned share indices; the coordinator host holds none | PASS |
| L5 | Every signer runs as its own user, NoNewPrivs=1, no capabilities, ProtectSystem=strict, same binary | PASS |
| E1–E8, REQ-a–d, N1–N4 | The cluster works end to end against the remote signers | PASS |

## What this does NOT give

- **Not infrastructure independence.** One physical machine. A host compromise, hypervisor
  bug, disk theft, or power or hardware failure affects all five signers together. Anyone
  root on the Mac can read every VM's disk and memory.
- **Same operator.** One person holds SSH to every signer host (L2 grants it to the
  operator IP), so compromising the operator is compromising every signer.
- **Same software.** Every signer runs the identical binary (L5). One bug or supply-chain
  compromise in it applies to all signers at once.
- **Same dealer.** One trusted dealer generated all shares (`docs/KEY_CEREMONY.md`). A
  dishonest or compromised dealer knows the whole key.
- **Two signers per VM on sig-a and sig-b.** Compromising either of those VMs yields **two**
  shares. Two such VMs, or one of them plus sig-c, reach the threshold of 3. The per-user
  isolation (L3) only protects against compromise of a signer *process*, not of the VM.
- **Shared network path.** All signer traffic crosses the same Multipass bridge on one host.
- **Emulated WAN only.** Latency in the benchmark is `tc netem`. Measured RTTs are in
  `rtt-L*.json` and are ~32–45 ms above the configured delay (NOTES N45).

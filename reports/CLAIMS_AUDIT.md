# Claims audit

Each row is a claim from the papers about this project (Paper 1, Paper 3) or the KEP-740
findings. The verdict applies to **what the claim says about the prototype the papers
describe** (the FROST prototype, now in `legacy/frost/`, code as of commit `7692d5a` on
`main`). Where the rebuilt system (threshold RSA, branch `fix/threshold-rsa`) changes the
answer, the last column says what is true now, with evidence.

Verdicts: **TRUE**, **FALSE**, **NEEDS-REWORD**, **SUPERSEDED**. Legacy line numbers refer
to `legacy/frost/…` (each file there gained a two-line `//go:build legacy` header; the
original code is at `git show 7692d5a:<path without legacy/frost/>`).

| # | Claim (source) | Verdict | Evidence | What is true now (rebuilt system) |
|---|---|---|---|---|
| 1 | "The coordinator holds no signing key material" (Paper 3 §3.1, §8.3, §10) | **FALSE** | The old coordinator loaded a P-256 **private** key and signed every JWT with it: `legacy/frost/cmd/grpc-proxy/main.go:228` (`signing.NewECDSAKey`), `:205` (`ecKey.SignES256`). Its image shipped `ecdsa-signing.pem`, `frost-keys.json` (all 5 FROST shares) and `proxy.key`: `git show 7692d5a:deploy/docker/Dockerfile.proxy` lines 18–22. It also decoded all 5 secret shares: `git show 7692d5a:internal/coordinatorstate/bootstrap.go` lines 40–56 (D5) | **TRUE now.** The coordinator loads only `public-meta.json`, the policy and its mTLS keys (I3). Evidence: T12 `TestCoordinatorHasNoSecretTypes` (go list + AST), T13 `scripts/check-images.sh` plus negative control, L4 (coordinator host holds no share), C4 (Phase 9) |
| 2 | "Converts the Schnorr signature to IEEE P1363" (Paper 3 §3.2) | **FALSE** | No conversion of a Schnorr signature exists, and none is possible (`legacy/frost/README.md` "Why FROST was abandoned" §3). The P1363 encoding in the code is applied to a **separate ECDSA** signature: `legacy/frost/internal/signing/ecdsa.go:61–71` (`SignES256`, "IEEE P1363 format: R ‖ S"). The FROST aggregate was computed and discarded: `legacy/frost/cmd/grpc-proxy/main.go:198` (`_, err = aggregateSignature(...)`) | Not applicable: RS256 threshold RSA. There is no signature conversion; the combined Shoup signature **is** the RSASSA-PKCS1-v1_5 signature (spike, T1) |
| 3 | "Group public key exported in PKIX to `ecdsa-signing.pem` for FetchKeys" (Paper 3 §5.4) | **FALSE** | `ecdsa-signing.pem` held an **EC PRIVATE KEY** (verified without printing it: file header `-----BEGIN EC PRIVATE KEY-----`, NOTES/Phase 1; history commits 93f0dc5, 496ce83). FetchKeys published **the public half of that single coordinator key**: `legacy/frost/cmd/grpc-proxy/main.go:234` (`ecKey.PublicKeyPKIX()`). The FROST group key is a Ristretto255 point (`legacy/frost/cmd/keygen/main.go:19`) and cannot be PKIX-encoded as a P-256 key | **TRUE now in substance:** FetchKeys returns the **threshold group public key** (PKIX RSA-2048, kid = b64url(SHA-256(PKIX)[:16])). Evidence: T1 `TestIssuedJWTVerifiesWithFetchKeysOnly`, `TestFetchKeysPKIXRoundTrip` (R-a), E5 (`/openid/v1/jwks` modulus equals public-meta) |
| 4 | "Min. compromises to forge: 3" and "Coordinator compromise → requires t compromises" (Paper 3 Tables 1, 4) | **FALSE** (for the prototype) | One file (`ecdsa-signing.pem`) or one coordinator compromise sufficed to forge any token, offline and forever (claims 1 and 3). Worse, the key and all shares were committed to this public repository (`reports/HISTORY_PURGE.md`) | **NEEDS-REWORD for the rebuilt system.** *Offline* forgery needs ≥ 3 shares (T4, C5); coordinator compromise yields no offline forgery (C4). But a compromised coordinator can obtain **policy-compliant** tokens while in control (online oracle, C2(b)), and on the Level 1 topology 2 VM compromises (sig-a + sig-b) yield 4 shares. Correct wording: "offline forgery requires compromising ≥ 3 signers; while an attacker controls the coordinator it can obtain tokens for claims the signers' policy allows" (docs/THREAT_MODEL.md §4, §7) |
| 5 | "Test A: coordinator compromise cannot enable forgery" (Paper 3 §4.4) | **FALSE** | Given claims 1 and 3, a coordinator compromise *was* a forgery. Nothing in the prototype's tests exercised a compromised coordinator. The old signers signed whatever the coordinator sent (D11): `git show 7692d5a:cmd/signer/main.go` `signHandler` has no claims validation | **NEEDS-REWORD.** Evidence now: T7 `TestSignerPolicyRejectsCoordinatorBypass` (a caller with the real coordinator cert gets 0/5 shares for policy-violating claims), T6, N14; C2 and C4 in Phase 9. Say "cannot enable forgery of policy-violating tokens, nor any forgery after control is lost" |
| 6 | "Schnorr-signed JWT serialized as ES256" (Paper 1 §VII-B, §VIII-C) | **FALSE** | The JWTs were ECDSA-signed by one key and labelled ES256: `legacy/frost/cmd/grpc-proxy/main.go:180` (`"alg":"ES256"`), `:205`. A Schnorr signature cannot verify as ES256 under any key (`legacy/frost/README.md` §3) | Tokens are RS256, and their signature is the threshold-combined RSA signature (I1: T1, T2, E1) |
| 7 | "Threshold ECDSA less practical than FROST" (Paper 1 §VII-B) | **NEEDS-REWORD** | The comparison is framed wrongly for this use: FROST **cannot** produce any signature Kubernetes accepts (RS256/ES256/ES384/ES512 only: `pkg/serviceaccount/externaljwt/plugin/plugin.go:180` at v1.36.5), so it is not a practical option at all. Threshold ECDSA *could* produce ES256, but the maintained Go implementation (`taurusgroup/multi-party-sig`, CGGMP21) supports only secp256k1, which Kubernetes does not accept (no ES256K) | The implemented option is threshold **RSA** (Shoup), which yields RS256 natively and needs no interactive nonce rounds. Suggested wording: "for Kubernetes, FROST is excluded by the algorithm allow-list; threshold ECDSA on P-256 lacks a maintained Go implementation; threshold RSA works without changes to Kubernetes" |
| 8 | "40× improvement" (Paper 3 §1, §4.2, §10) | **NEEDS-REWORD** | The ratio is not a constant. For independent node compromise probability p, P(single key compromised) = p and P(≥ t of n compromised) ≈ C(n,t)·p^t for small p, so the ratio is **1 / (C(n,t)·p^(t−1)) = 1/(10p²)** for t=3, n=5. It **equals 40 only at p = 0.05**; at p = 0.01 it is 1000, at p = 0.1 it is 10. It also assumes independent compromise, which the tested deployments do not provide (docs/THREAT_MODEL.md §6) | Report the formula and the independence assumption, not "40×" |
| 9 | KEP-740 Finding 1: quoted proto wording of `SignJWTRequest.claims` | **TRUE** (wording) / **NEEDS-REWORD** (framing) | Exact upstream text at **v1.36.5** (`ad950d1cc78b0183c476bd4d3f1934c104229727`), `staging/src/k8s.io/externaljwt/apis/v1/api.proto` lines 50–53: `// URL-safe base64 wrapped payload to be signed.` / `// Exactly as it appears in the second segment of the JWT` / `string claims = 1;`. **Unchanged on master** (`71792b894080db4ff05f9f73879dea6e624e62a1`); the only later commit touching the file is `693b7b3db83a` (2026-08-29, "gofmt: fix to pass with go1.27"). Issue **#141670** (open, needs-triage, 1 comment) itself says the wording is "technically correct and, on a careful read, unambiguous" and proposes an optional warning. PR **#141687** ("warn against re-encoding claims in proto; add encoding contract test") is **open, not merged** (needs-ok-to-test, 3 comments) | Classification: **documentation gap plus implementer error**, not a spec defect. Phrase as "found and reported upstream (#141670, PR #141687 open); any fix is the maintainers' decision". The rebuilt coordinator uses `claims` as-is (N8; T1, E1) |
| 10 | Finding 2: DER vs IEEE P1363 for ECDSA signatures (issue #141669) | **TRUE as an integration lesson; irrelevant to the signing path now** | RFC 7518 §3.4 requires `R ‖ S`. The v1.36.5 proto says only "Already wrapped in URL-safe base64, exactly as it appears in the final segment of the JWT" (`api.proto` lines 65–67). Issue **#141669** (open, needs-triage, 4 comments) asks for an explicit note. Classification: **documentation gap** | The rebuilt system signs RS256, so there is no ECDSA encoding choice. Keep this as a documented integration lesson only |
| 11 | Paper 3 Tables 2–3: every latency/overhead number | **SUPERSEDED** | The old numbers measured a single-key ECDSA signer behind extra gRPC/nginx/socat hops against the in-tree signer, so algorithm and architecture changed at once (D12). They came from single `kubectl` invocations, N ≈ 20. The "36 ms vs 70 ms" inconsistency is a **bimodal** latency, about 35 ms or about 69 ms per token, present **in the in-tree baseline too**, which has no FROST at all (`legacy/frost/benchmark/results/baseline_benchmark_20260619_201519.txt`: runs of 69 ms and 35–37 ms). 36 ms is an average (`frost_benchmark_20260619_200532.txt:37`: "Avg: 36ms … P95: 69ms"); 70 ms is one run from the slow mode (`benchmark_20260608_224047.txt:44`: "Token with 3/5 signers: 70ms"). Neither number characterises the signing path | No prototype number may be reused. New numbers: **Phase 7A** (`benchmark/results/20260926T172317Z-8ebb806-7A-single-m7i-flex.large/summary.md`), labelled **single host, 2 vCPU (m7i-flex.large), all components co-located; CPU-contended**: B0 in-tree / B1 single-key external signer on the same unix-socket + nginx + 3-replica path (benchmark-only, N54) / T threshold, all on the same host, 3 runs, N=1000 after 100 warm-up. Median-of-runs client p50 at c=1: B0 2.6 ms, B1 3.1 ms, T 41.2 ms (optimistic-hedged) to 82.2 ms (strict-all); B1/B0 = 1.18–1.32× and T/B1 = 13–48× across c=1–50. T was CPU-bound (≈12 tokens/s strict, ≈21 optimistic) and failed N49-1 and N49-3 in all 4 variants. **Phase 7B** (`benchmark/results/20260926T194921Z-8be7ca6-multihost-L2/summary.md`): **T only, one run per configuration**, independence **5 regions, one provider, one account, one operator, one build, one dealer**. c=1 client p50 148–160 ms at a measured quorum RTT of 125 ms (all 5 signers) and 207–216 ms at 185 ms (far quorum); optimistic passed N49 in all 4 cases, strict failed N49-3 in 3 of 4. 7B has **no same-session B0/B1**, so **no comparison against default Kubernetes may be drawn from 7B**. The final numbers and the B0/B1/T comparison come from Phase 7C (NOTES N62). |

## Additional claims found during the rebuild

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 12 | "Vault protects the shares" (prototype README) | **FALSE** | Every signer also loaded the full all-shares file; a Vault-fetched share only overwrote one entry (D6: `git show 7692d5a:internal/froststate/bootstrap.go` `Init`). The Vault dev root token and the share-file password were hardcoded and committed (D7, `reports/HISTORY_PURGE.md`) |
| 13 | "mTLS between coordinator and signers" (prototype README) | **NEEDS-REWORD** | The old signer used `ClientAuth: tls.RequestClientCert` (never verifies) and fell back to plain HTTP when the CA file was missing; all signers shared one cert and key, which was committed (D8, NOTES N10). Now: TLS 1.3, `RequireAndVerifyClientCert`, exactly-one-SAN identities per role (`TestTLSRejectsClientWithoutCoordinatorSAN`, `TestTCPListenerRequiresLBClientCert`, N1–N4) |
| 14 | Signers are independent / the threshold defends against host compromise | **NEEDS-REWORD** | Single host: all signers on one VM. Level 1: multi-VM on one physical host, one operator, one build (reports/INDEPENDENCE.md). Level 2: not done |

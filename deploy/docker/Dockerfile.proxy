FROM golang:1.25-alpine AS builder

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

RUN go mod tidy && go build -o /bin/grpc-proxy ./cmd/grpc-proxy/

FROM alpine:3.20

RUN mkdir -p /var/run/frost-k8s

COPY --from=builder /bin/grpc-proxy /bin/grpc-proxy
COPY --from=builder /app/data/frost-keys.json /app/data/frost-keys.json

WORKDIR /app

ENTRYPOINT ["/bin/grpc-proxy"]
